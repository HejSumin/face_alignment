class MultipleCascades(): #TODO Dataclass

    def __init__(self, cascades, S_mean, features_mean):
        self.cascades = cascades
        self.S_mean = S_mean
        self.features_mean = features_mean

    def predict(self, I):
        #TODO call apply_cascade function on every cascade here ...
        return
